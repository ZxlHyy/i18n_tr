# i18n_tr example

This is a minimal Flutter app that uses `i18n_tr`.

## Run

From the repository root:

```
cd example
flutter pub get
flutter run
```

## Notes

- The translations come from the generated maps in the main package. Run
  `dart run i18n_tr:generate` in the repository root before testing.
